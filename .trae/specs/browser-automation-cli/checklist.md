- [x] `browser_cli/` 目录结构完整，包含 controller.py、actions.py、parser.py、cli.py
- [x] `requirements.txt` 包含所有必要依赖
- [x] `BrowserController` 类可创建隐藏窗口、执行 JS 并同步返回结果
- [x] `BrowserController.wait_loaded()` 可正确等待页面加载完成
- [x] `AutomationActions.goto()` 可导航到 URL 并等待加载
- [x] `AutomationActions.click()` 可定位元素并触发点击
- [x] `AutomationActions.fill()` 可设置表单值并触发 input/change 事件
- [x] `AutomationActions.type_text()` 可逐字符输入并触发键盘事件
- [x] `AutomationActions.extract()` 可提取文本、属性、HTML
- [x] `AutomationActions.wait_for_selector()` 可等待元素出现
- [x] `AutomationActions.set_cookies()` 可注入 Cookie
- [x] `AutomationActions.get_cookies()` 可获取 Cookie
- [x] `AutomationActions.override_dialogs()` 可拦截原生对话框
- [x] `AutomationActions.screenshot()` 可截取页面图像
- [x] `AutomationActions.login()` 可完成登录流程
- [x] `AutomationActions.search()` 可完成搜索流程
- [x] `ContentParser` 可提取链接、表格、正则过滤
- [x] CLI `goto` 命令可正常执行
- [x] CLI `extract` 命令可正常执行
- [x] CLI `fill-form` 命令可正常执行
- [x] CLI `search` 命令可正常执行
- [x] CLI `set-cookies` 命令可正常执行
- [x] CLI `screenshot` 命令可正常执行
- [x] CLI `run` 命令可执行 YAML 剧本
- [x] 示例 YAML 剧本文件存在且格式正确
- [x] README.md 包含安装、使用说明和示例
- [x] 代码无语法错误，导入路径正确