- [x] `browser-cli multi-window` 命令存在且可创建多个窗口
- [x] `extract` 命令支持 `--wait-selector` 和 `--wait-dynamic`
- [x] `browser-cli fill-steps` 命令存在且支持多步操作
- [x] `browser-cli login` 命令存在且可完成登录
- [x] `search` 命令支持 `--extract-result` 和 `--wait-result`
- [x] 所有新增命令无语法错误
- [x] 所有新增命令在 --help 中有正确描述

> 验证日期：2026-08-01。命令与选项通过 `--help` + 代码交叉确认；search/extract/webui 已端到端实测通过。