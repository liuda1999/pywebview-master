# 项目介绍：pywebview 浏览器自动化套件（Browser Automation Toolkit）

---

## 第一章 这是什么？——一个会自己动手的"浏览器助手"

### 1.1 一句话说清楚

这个项目，是一个 **"看得见屏幕、会自己动手"的浏览器机器人**。

你可以给它下指令："打开 B 站 CSGO 官方赛事直播间"、"把这个表单填了"、"把这几页的数据提取出来"，它会**真的打开一个浏览器窗口**，像人一样完成这些事，然后把结果告诉你。

### 1.2 打个比方：机器人秘书

想象你雇了一个**能看见屏幕的机器人秘书**，它坐在一台装了浏览器的电脑前：

- **它的眼睛**：能"读"网页上的内容——标题、文字、表格、图片、链接，看得一清二楚；
- **它的手**：能点击按钮、往输入框里打字、勾选选项、滚动页面、上传文件、按键盘；
- **它的大脑**：听得懂三种指令——命令行（一条条命令）、剧本文件（一整套流程）、网页控制台（可视化遥控器）；
- **它的笔记本**：能把页面截图、把数据导出成 JSON、把操作过程录下来回放。

这个秘书**用的就是真实浏览器**（Windows 上是 Edge，macOS 上是 Safari 内核，Linux 上是 WebKit/CEF），窗口**看得见、能手动干预**——和你在屏幕前操作一模一样，网站根本察觉不到"这是机器人"。

### 1.3 它和普通"爬虫"有什么不一样？

| 对比项 | 传统爬虫（requests 等） | 本项目 |
| --- | --- | --- |
| 运行方式 | 后台模拟 HTTP 请求 | **真实打开浏览器窗口**，像人一样操作 |
| 动态网页 | 拿不到 JS 渲染后的内容 | 等页面渲染完再读，SPA 网站也没问题 |
| 网站识别 | 容易被识别为机器人 | 真实浏览器指纹，行为与真人一致 |
| 交互能力 | 基本只能"读" | 点击、填表、登录、上传、悬停、滚动，全能 |
| 观察方式 | 黑箱 | 全程可见，可随时手动接管 |

### 1.4 它能帮你做什么？（典型场景）

- **看直播/监控页面**：打开直播间、定时刷新、提取人气数据；
- **批量填表**：给几十个表单自动填姓名、电话、邮箱，提交后自动验证结果；
- **自动登录**：把账号密码交给它，自动完成登录流程并提取登录后的页面内容；
- **数据采集**：批量打开 URL 列表，提取标题/表格/链接/图片，导出 JSON；
- **重复性网页操作**：把日常重复的"打开网页 → 点击 → 输入 → 提取"录制成脚本，一键回放。

### 1.5 项目是怎么组成的？

整个项目分两层：

1. **底层**：`pywebview` **社区定制版**——一个把系统网页引擎（Edge/WebKit/CEF）包装成 Python 窗口的框架，本项目基于它深度定制；
2. **上层**：`browser_cli` **自动化工具包**——在定制版之上封装的一套"机器人秘书"工具：命令行（CLI）、自动化动作库、数据提取器、多窗口管理、YAML 剧本系统、Web 控制面板。

接下来各章详细介绍上层工具包的功能。

---

## 第二章 功能总览：25 个命令行命令

所有功能都可以通过命令行触发，命令分 8 大类：

### 2.1 导航与提取

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `goto` | 导航到指定 URL | `goto "https://example.com"` |
| `extract` | 用 CSS 选择器提取页面内容 | `extract "h1" --url "..." --format json` |
| `snapshot` | 输出页面 DOM 文本快照（标题、URL、可见文本、表单信息） | `snapshot --url "https://example.com"` |

### 2.2 表单与交互

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `fill-form` | 一次填写多个表单字段 | `fill-form --url "..." --field "input[name=q]:关键词"` |
| `fill-steps` | 分步执行自动化（fill/click/wait/type/select/extract） | `fill-steps --url "..." --step "fill:#name:张三" --step "click:button"` |
| `hover` | 鼠标悬停，触发 mouseenter/mouseover | `hover "a" --url "https://example.com"` |
| `scroll-to` | 滚动到指定元素位置 | `scroll-to "footer" --url "..."` |
| `scroll-down` | 向下滚动指定像素 | `scroll-down 500 --url "..."` |
| `press` | 模拟键盘按键（Enter/Tab/Escape/ArrowDown 等） | `press "Enter" --url "..."` |
| `upload` | 触发文件上传对话框 | `upload "input[type=file]" ./file.pdf --url "..."` |

### 2.3 搜索与登录

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `search` | 打开搜索页、输入关键词、提取搜索结果 | `search --url "https://bing.com" --query "pywebview" --input "input[name=q]"` |
| `login` | 导航到登录页，自动填用户名密码并提交，可选提取登录后内容 | `login --url "..." --username "user" --password "pass"` |

### 2.4 截图与导出

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `screenshot` | 页面/元素截图 | `screenshot --url "..." -o page.png --selector ".main"` |
| `pdf` | 打开浏览器打印对话框，手动另存为 PDF | `pdf output.pdf --url "..."` |

### 2.5 录制与回放

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `record` | 交互式录制用户操作，生成 JSON 脚本 | `record --url "..." -o script.json` |
| `replay` | 回放录制的操作脚本，可调速 | `replay script.json --speed 1.5` |

### 2.6 等待与同步

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `wait-for-timeout` | 等待指定毫秒数 | `wait-for-timeout 3000` |
| `wait-for-url` | 等待 URL 匹配 glob 模式（如 `**/success`） | `wait-for-url "**/success" --url "https://example.com"` |

### 2.7 多窗口与批量

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `multi-window` | 同时打开多个浏览器窗口 | `multi-window --urls "url1,url2" --stay-open 30` |
| `batch` | 批量执行文件中的 URL 列表（每行一个 URL） | `batch urls.txt --extract "h1" -o result.json` |
| `webui` | 启动 Web 控制面板 | `webui --port 8000 --max-windows 5` |

### 2.8 环境与诊断

| 命令 | 功能 | 示例 |
| --- | --- | --- |
| `doctor` | 系统环境诊断（Python/pywebview/WebView2 等依赖检查） | `doctor` |
| `browsers` | 列出系统已安装的浏览器 | `browsers` |
| `set-cookies` | 为站点设置 Cookie | `set-cookies --cookie "name=value" --url "..."` |
| `run` | 执行 YAML 格式的自动化剧本 | `run --playbook scenario.yaml` |

---

## 第三章 自动化动作库：机器人的"手"

底层动作库（`actions.py`）提供 **20 个核心动作方法**，是填表、点击、截图等命令的引擎：

### 3.1 核心亮点：防"坑"的智能填表

`fill` 方法是本项目的招牌功能，它解决了网页自动化最常见的三大翻车场景（详见 `TROUBLESHOOTING.md`）：

1. **防异步覆盖**：很多现代网站（SPA）是"边加载边初始化"的——你填完的值，页面一刷新就被清空。`fill` 会在填入后**自动验证值是否还在**，被覆盖就自动重填（最多重试 6 次），直到真正填进去；
2. **防隐藏表单**：有些网站（如百度的搜索框）把表单藏在隐藏的容器里，普通自动化根本找不到。`fill` 会自动**恢复隐藏的祖先容器**，让表单变得可操作；
3. **防零尺寸元素**：元素在 DOM 里但尺寸为 0（被 `display:none` 隐藏）时，普通库点击会失败，本项目有专门的处理流程。

### 3.2 动作方法清单

| 类别 | 方法 | 说明 |
| --- | --- | --- |
| 交互 | `click` / `hover` / `press_key` | 点击、悬停、键盘按键 |
| 输入 | `fill` / `type_text` / `select_option` | 智能填表（防覆盖/防隐藏）、逐字输入、下拉选择 |
| 表单 | `submit_form` / `upload_file` | 提交表单、文件上传 |
| 提取 | `extract` / `get_dom_json` | 选择器提取、DOM 树 JSON |
| Cookie | `set_cookies` / `get_cookies` | 写入/读取 Cookie |
| 截图 | `screenshot` / `screenshot_element` | 整页/元素截图 |
| 弹窗 | `override_dialogs` / `get_dialog_messages` | 接管 JS 弹窗（alert/confirm），收集消息 |
| 滚动 | `scroll_to_selector` / `scroll_by` | 滚动到元素 / 按像素滚动 |
| JS | `exec_js_async` | 异步执行任意 JavaScript |
| 收尾 | `close` | 关闭窗口 |

---

## 第四章 数据提取器：机器人的"眼睛"

`parser.py` 提供 **6 个数据提取方法**，配合 `extract` 命令使用：

| 方法 | 提取内容 |
| --- | --- |
| `get_links` | 页面所有链接（href + 文本） |
| `get_tables` | 所有表格（转为结构化数据） |
| `get_all_text` | 页面全部可见文本 |
| `get_meta_tags` | 页面 meta 标签（标题、描述、关键词等） |
| `get_forms` | 所有表单结构（字段名、类型、默认值） |
| `get_images` | 所有图片（src + alt） |

**动态页面等待机制**：`extract` 命令支持 `--wait-selector`（等待某个元素出现）和 `--wait-dynamic`（等待动态内容加载），确保 SPA 页面渲染完成后才提取，不会读到半成品页面。

---

## 第五章 多窗口管理：一个"多屏秘书团"

`controller.py` 提供三个核心类：

| 类 | 职责 |
| --- | --- |
| `BrowserController` | 单个浏览器窗口的完整控制（导航、JS、等待、关闭） |
| `MultiWindowManager` | 多窗口管理器：同时打开 N 个窗口，各自独立导航 |
| `WindowPool` | 窗口池：维护一批常驻窗口，支持切换激活窗口、按槽位操作 |

能力包括：

- **同时打开多个窗口**，各自加载不同 URL（`multi-window --urls "url1,url2"`）；
- **窗口池常驻**：启动时创建固定数量窗口，后续操作按"激活窗口/槽位"精确指定目标；
- **独立控制**：每个窗口有独立的执行环境，可单独导航、执行 JS、截图、关闭；
- **批量导航**：`goto_all` 一次给所有窗口分配 URL，`get_all_urls` 批量查看状态。

> 注：本项目对 pywebview 6.2.1 的多窗口事件缺陷做了**内置补丁**（`patches.py`），解决了多窗口下"导航完成事件不派发"导致的窗口列表/切换卡死问题（详见 TROUBLESHOOTING 1.4 节）。

---

## 第六章 剧本系统：把流程写成"剧本"一次演完

### 6.1 YAML 剧本（`run` 命令）

把一整套操作写成 YAML 文件，一条命令全部执行。示例（`examples/scenario.yaml`）：

```yaml
name: "httpbin 表单提交与提取演示"
steps:
  - action: goto
    url: "https://httpbin.org/forms/post"

  - action: wait
    selector: "form"
    timeout: 10

  - action: fill
    selector: 'input[name="custname"]'
    value: "张三"

  - action: fill
    selector: 'input[name="custemail"]'
    value: "zhangsan@example.com"

  - action: click
    selector: 'input[name="size"][value="medium"]'

  - action: select
    selector: 'select[name="delivery"]'
    value: "fast"

  - action: extract
    selector: ".success-message"
    save: result.json

  - action: screenshot
    path: "result.png"
```

支持的动作：`goto` / `wait` / `fill` / `click` / `type` / `select` / `extract` / `screenshot` 等。

### 6.2 录制与回放（`record` / `replay`）

不想手写剧本？可以**打开浏览器手动操作一遍**，`record` 命令把每一步操作录制成 JSON 脚本；之后用 `replay` 一键回放（支持 `--speed` 调速）。"录一次，放一万次"。

### 6.3 批量处理（`batch`）

把 URL 列表写进文本文件（每行一个），`batch` 命令逐个打开、逐个提取、汇总输出 JSON。

---

## 第七章 Web 控制面板：网页上的"遥控器"

`webui` 命令启动一个 Flask 控制面板（默认端口 8125），在**任意浏览器**里通过网页遥控整套自动化工具：

### 7.1 控制面板页面

- 可视化窗口列表，显示每个窗口当前 URL、标题、状态；
- 一键切换激活窗口、打开新窗口；
- 导航、截图、填表、点击、提取，全都能在网页上点按完成。

### 7.2 HTTP API（15 个端点）

| 端点 | 功能 |
| --- | --- |
| `GET /` | 控制面板页面 |
| `GET /api/windows` | 窗口列表 |
| `POST /api/windows/switch` | 切换激活窗口 |
| `POST /api/goto` | 导航到 URL |
| `GET /api/status` | 当前状态（激活窗口、URL、标题） |
| `POST /api/extract` | 提取页面内容 |
| `POST /api/exec_js` | 执行任意 JavaScript |
| `POST /api/screenshot` | 截图 |
| `POST /api/fill` | 填写表单字段 |
| `POST /api/click` | 点击元素 |
| `POST /api/links` | 提取页面链接 |
| `POST /api/login` | 自动登录 |
| `POST /api/search` | 搜索并提取结果 |
| `POST /api/fill-steps` | 分步自动化操作 |
| `POST /api/stop` | 停止窗口 |

API 使用 Bearer Token 鉴权（启动时生成），第三方程序也能直接调用——也就是说，**任何会发 HTTP 请求的程序都可以当这套工具的遥控器**。

---

## 第八章 工程特性与可靠性

| 特性 | 说明 |
| --- | --- |
| `patches.py` | 多窗口 NavigationCompleted 事件补偿补丁，根治 pywebview 6.2.1 多窗口卡死（TROUBLESHOOTING 1.4） |
| `run_visible.py` | 强制窗口可见启动器：解决启动环境 SW_HIDE 导致窗口不可见的问题，桌面必见 |
| `doctor` 诊断 | 一键检查 Python / pywebview / WebView2 等环境依赖是否就绪 |
| `TROUBLESHOOTING.md` | 真实环境踩坑与排障手册：窗口不可见、SPA 覆盖填值、隐藏表单、多窗口卡死等问题的定位方法 |
| 跨平台 | Windows（WebView2）/ macOS（WKWebView）/ Linux（GTK3+WebKit2 或 CEF） |
| 安全 | 输出路径规范化，拒绝路径遍历（`..`）逃逸 |

---

## 第九章 快速上手

```bash
# 1. 环境要求：Python >= 3.10；Windows 需 Edge WebView2 Runtime（Win10/11 自带）

# 2. 安装（在 pywebview 定制版根目录）
pip install -e ./browser_cli

# 3. 环境诊断
python -m browser_cli.cli doctor

# 4. 打开一个网页
python -m browser_cli.cli goto --url "https://www.bilibili.com"

# 5. 提取页面标题
python -m browser_cli.cli extract "title" --url "https://example.com"

# 6. 自动填表
python -m browser_cli.cli fill-form --url "https://example.com/form" \
  --field "input[name=name]:张三" --field "input[name=email]:test@test.com"

# 7. 多步自动化
python -m browser_cli.cli fill-steps --url "https://example.com/form" \
  --step "fill:#name:张三" \
  --step "click:button[type=submit]" \
  --step "extract:.success-message"

# 8. 多窗口同时打开
python -m browser_cli.cli multi-window --urls "https://a.com,https://b.com" --stay-open 30

# 9. 启动 Web 控制面板（默认 5 窗口，可自定义端口）
python -m browser_cli.cli webui --port 8000 --max-windows 10

# 10. 执行 YAML 剧本
python -m browser_cli.cli run --playbook examples/scenario.yaml
```

> 提示：在 SW_HIDE 启动环境（某些远程/计划任务环境）中，窗口可能不可见，请改用 `scripts/run_visible.py` 包装启动。

---

## 第十章 技术栈一览

| 层 | 技术 |
| --- | --- |
| 窗口引擎 | pywebview 社区定制版（WebView2 / WKWebView / GTK+WebKit2 / CEF） |
| 命令行 | Click |
| 配置/剧本 | PyYAML |
| 数据提取 | BeautifulSoup4 |
| 截图 | Pillow |
| 控制面板 | Flask + 原生 HTML/JS |
| 语言 | Python >= 3.10 |

---

*本文档基于项目实际代码功能编写（25 个 CLI 命令、20 个动作方法、6 个提取方法、3 个核心类、15 个 API 端点），与 README.md、TROUBLESHOOTING.md 相互补充。*
